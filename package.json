{
  "name": "examprep",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "build:web": "npx expo export --platform web",
    "predeploy": "npm run build:web && cp -r assets dist/assets && node scripts/fix-base-path.js && touch dist/.nojekyll",
    "deploy": "node ./node_modules/gh-pages/bin/gh-pages.js -d dist --dotfiles -r https://github.com/yijshen8d/MegExam.git -b gh-pages -m \"deploy: update [skip ci]\""
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "2.2.0",
    "expo": "~54.0.33",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "react-native": "0.81.5",
    "react-native-web": "^0.21.0"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
